<Window x:Class="WeekCalendarTray.AddEventWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Event"
        Width="420"
        Height="470"
        AllowsTransparency="False"
        Background="Transparent"
        Foreground="{DynamicResource PrimaryTextBrush}"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None">
    <Window.Resources>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="0,0,0,6" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Padding" Value="8,5" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="12,4" />
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextBrush}" />
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="FontSize" Value="13" />
        </Style>
    </Window.Resources>

    <Border Background="{DynamicResource WindowSurfaceBrush}"
            BorderBrush="{DynamicResource WindowBorderBrush}"
            BorderThickness="1"
            CornerRadius="8">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid MouseLeftButtonDown="Window_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="32" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Foreground="{DynamicResource PrimaryTextBrush}"
                           FontFamily="Segoe UI"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Text="Add event" />
                <TextBlock Margin="0,6,0,0"
                           Foreground="{DynamicResource MutedTextBrush}"
                           FontFamily="Segoe UI"
                           FontSize="12"
                           Text="Local events stay on this PC and are not synced to iCal calendars."
                           TextWrapping="Wrap" />
            </StackPanel>
            <Button Grid.Column="1"
                    Width="30"
                    Height="30"
                    Click="Cancel_Click"
                    Content="x" />
        </Grid>

        <StackPanel Grid.Row="1" Margin="0,18,0,0">
            <TextBlock Style="{StaticResource LabelStyle}" Text="Title" />
            <TextBox x:Name="TitleTextBox" />
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="120" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Style="{StaticResource LabelStyle}" Text="Date" />
                <TextBox x:Name="EventDateTextBox" />
            </StackPanel>

            <CheckBox x:Name="AllDayCheckBox"
                      Grid.Column="1"
                      Margin="18,24,0,0"
                      Checked="AllDayCheckBox_Changed"
                      Content="All day"
                      Unchecked="AllDayCheckBox_Changed" />
        </Grid>

        <Grid x:Name="TimeGrid" Grid.Row="3" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Style="{StaticResource LabelStyle}" Text="Start" />
                <TextBox x:Name="StartTimeTextBox" Text="09:00" />
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Style="{StaticResource LabelStyle}" Text="End" />
                <TextBox x:Name="EndTimeTextBox" Text="10:00" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="4" Margin="0,14,0,0">
            <TextBlock Style="{StaticResource LabelStyle}" Text="Location" />
            <TextBox x:Name="LocationTextBox" />
        </StackPanel>

        <StackPanel Grid.Row="5" Margin="0,14,0,0">
            <TextBlock Style="{StaticResource LabelStyle}" Text="Details" />
            <TextBox x:Name="DescriptionTextBox"
                     Height="78"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" />
        </StackPanel>

        <TextBlock x:Name="StatusText"
                   Grid.Row="6"
                   Margin="0,12,0,12"
                   Foreground="{DynamicResource MutedTextBrush}"
                   FontFamily="Segoe UI"
                   FontSize="12"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="7"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Click="Save_Click" Content="Add" />
            <Button Margin="8,0,0,0" Click="Cancel_Click" Content="Cancel" />
        </StackPanel>
    </Grid>
    </Border>
</Window>
